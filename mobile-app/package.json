{
  "name": "montenoir-vip-app",
  "version": "1.0.0",
  "private": true,
  "description": "Native app wrapper (Capacitor) for Montenoir VIP, pointing at the live site.",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
